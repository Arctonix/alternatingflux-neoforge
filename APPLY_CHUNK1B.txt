CHUNK 1b — relay placement + name fixes
========================================
Two small fixes on top of Chunk 1:
  1. Blockstate now includes the waterlogged property (was missing), which is
     why the relay could only face one direction. Now it faces the clicked
     surface like IE's own connectors (all 6 sides).
  2. Lang key now matches the block's registry name (connector_af_relay), so it
     shows "AF Wire Relay" instead of the raw translation key.

APPLY (overwrites two files with corrected versions):
  cd ~/Downloads
  unzip -o chunk1b.zip -d ~/Documents/"AF mod"/afmod
  cd ~/Documents/"AF mod"/afmod
  ./gradlew runClient

TEST:
  - Relay name reads "AF Wire Relay".
  - Place it on floor, wall, and ceiling - it should orient to whatever face
    you click, same as a normal HV relay.

IF GOOD:
  git add -A
  git commit -m "Chunk 1b: fix relay 6-way placement (waterlogged state) + lang key"
